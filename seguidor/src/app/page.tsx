import withAuthProtection from "@/helpers/HOC";

export const Home = () => {
  

  return (
    <div className="relative h-full w-full z-10 flex flex-col min-h-screen">
      <main>
        
      </main>
    </div>
  );
}
export default withAuthProtection(Home)